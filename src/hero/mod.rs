//! Hero's half of the app's MVC layers: this template's worked CRUD
//! example, built on top of `crate::generic` (mirrors template-fastapi's
//! `src/app/` package). See `src/README.md`'s "Generic vs. Hero-specific
//! split" and each submodule's own `README.md`.

pub mod controllers;
pub mod models;
pub mod repositories;
pub mod views;

//! Hero's Model layer: the SeaORM entity for this template's example
//! CRUD resource. See `crate::generic::models` for the resource-agnostic
//! `HasId` contract Hero's entity implements.

pub mod hero;
